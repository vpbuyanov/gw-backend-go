version: '3.9'

services:
  server:
    build: .
    image: buyanse/gw-backend-go:latest
    container_name: gw-backend-go
    env_file:
      - ../.env
    command: ["go", "run", "cmd", "migrate", "main.go"]
